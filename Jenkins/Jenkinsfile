pipeline {
    agent any

tools {
        nodejs 'nodejs'  // 'nodejs' is the name you gave it in the Global Tool Configuration
    }

    stages{
        stage('git checkout'){
            steps{
                git branch: 'main', url: 'https://github.com/Ashikasalian/devops_project.git'


            }
        }
    }
}